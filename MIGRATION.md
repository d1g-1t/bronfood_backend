# 🔄 Миграция проекта - Объединение Backend и Frontend

## ✅ Что было сделано

### 1. Структура проекта
Проект был реорганизован из раздельных репозиториев в единую монорепозиторную структуру:

```
bronfood_backend/
├── backend/           # Django backend (было отдельным репо)
├── frontend/          # React frontend (было отдельным репо)
├── .github/
│   └── workflows/     # Новые безопасные CI/CD workflows
├── docker-compose.yml # Общий оркестратор
└── Makefile          # Команды управления проектом
```

### 2. GitHub Actions - Безопасные Workflows

**Удалено (старые небезопасные workflows):**
- ❌ `backend/.github/workflows/main.yml` - деплоил на чужой сервер
- ❌ Публикация в Docker Hub `solodnikov/bronfood_backend`
- ❌ SSH подключение к серверу `45.82.14.98`

**Создано (новые безопасные workflows):**
- ✅ `.github/workflows/ci.yml` - полный CI/CD пайплайн
- ✅ `.github/workflows/code-quality.yml` - проверка качества кода

### 3. Что делают новые workflows

#### `ci.yml` - запускается при push в `develop_next` или `main`
1. **Backend Tests:**
   - Устанавливает Python 3.11
   - Запускает flake8 и black
   - Поднимает PostgreSQL в Docker
   - Выполняет миграции
   - Запускает Django тесты

2. **Frontend Tests:**
   - Устанавливает Node.js 18
   - Запускает ESLint
   - Собирает production build
   - Сохраняет артефакты сборки

3. **Docker Build:**
   - Собирает образы backend и frontend **локально**
   - НЕ публикует в Docker Hub
   - Проверяет корректность docker-compose.yml

4. **Security Check:**
   - Сканирует проект на уязвимости с помощью Trivy

#### `code-quality.yml` - запускается при любом push/PR
- Проверяет качество кода (линтеры, форматтеры)
- Быстрая проверка без запуска тестов

## 🚀 Следующие шаги

### Шаг 1: Проверьте изменения

```bash
cd c:/dev/bronfood_backend

# Посмотрите что изменилось
git status

# Посмотрите новые workflows
cat .github/workflows/ci.yml
cat .github/workflows/code-quality.yml
```

### Шаг 2: Добавьте изменения в Git

```bash
# Добавляем все новые файлы
git add .github/
git add .gitignore
git add README.md
git add Makefile
git add docker-compose.yml
git add docker-compose.dev.yml
git add backend/
git add frontend/
git add *.sh
git add *.bat

# Проверяем что будет закоммичено
git status
```

### Шаг 3: Создайте коммит

```bash
git commit -m "feat: merge backend and frontend into monorepo

- Объединены backend и frontend в единую структуру
- Добавлены новые безопасные GitHub Actions workflows
- Удалены старые workflows с деплоем на чужой сервер
- Добавлен Makefile для удобного управления проектом
- Обновлен README с новой структурой проекта"
```

### Шаг 4: Запушьте в ваш репозиторий

**Вариант A: Пуш в новую ветку (безопаснее, рекомендуется):**
```bash
# Создаём новую ветку
git checkout -b feature/monorepo-structure

# Пушим в новую ветку
git push origin feature/monorepo-structure

# После этого создайте Pull Request на GitHub
# для ревью изменений перед мержем в develop_next
```

**Вариант B: Пуш напрямую в develop_next:**
```bash
# Убедитесь что вы на правильной ветке
git branch

# Пушим
git push origin develop_next
```

### Шаг 5: Проверьте GitHub Actions

После пуша:
1. Откройте https://github.com/d1g-1t/bronfood_backend/actions
2. Убедитесь что workflows запустились
3. Проверьте что все проверки проходят успешно

## ⚙️ Настройка для будущего деплоя (опционально)

Если позже захотите настроить автоматический деплой на **ваш** сервер:

### 1. Добавьте GitHub Secrets

В настройках репозитория (Settings → Secrets and variables → Actions):

- `DOCKER_USERNAME` - ваш username на Docker Hub
- `DOCKER_PASSWORD` - ваш токен на Docker Hub
- `SERVER_HOST` - IP вашего сервера
- `SERVER_USERNAME` - SSH username
- `SERVER_SSH_KEY` - SSH приватный ключ

### 2. Создайте новый workflow для деплоя

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Production

on:
  workflow_dispatch:  # Ручной запуск
  push:
    branches:
      - main  # Только из main ветки
    tags:
      - 'v*'  # Или при создании релиза

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # Здесь будет логика деплоя на ваш сервер
```

## 🔍 Проверка безопасности

**Что проверить перед пушем:**

```bash
# 1. Убедитесь что старые workflows не будут выполняться
find . -name "*.yml" -path "*/.github/workflows/*" -type f

# Должны быть только:
# ./.github/workflows/ci.yml
# ./.github/workflows/code-quality.yml

# 2. Проверьте что в workflows нет чужих серверов/Docker Hub аккаунтов
grep -r "solodnikov" .github/ || echo "✅ Нет упоминаний чужого Docker Hub"
grep -r "45.82.14.98" .github/ || echo "✅ Нет упоминаний чужого сервера"

# 3. Проверьте что .gitignore игнорирует старые workflows
git check-ignore backend/.github/workflows/main.yml && echo "✅ Старые workflows игнорируются"
```

## 📝 Важные замечания

1. **Старые workflows в `backend/.github/` и `frontend/.github/` теперь игнорируются** `.gitignore`
2. **GitHub Actions смотрит только в `.github/workflows/` в корне** проекта
3. **Новые workflows НЕ публикуют образы и НЕ деплоят** автоматически
4. **Все проверки запускаются локально** в GitHub Actions runner'ах

## 🆘 Если что-то пошло не так

### Отменить все изменения:
```bash
git reset --hard HEAD
git clean -fd
```

### Вернуться к предыдущему состоянию:
```bash
git reflog  # Найдите нужный коммит
git reset --hard <commit-hash>
```

### Удалить случайно запушенную ветку:
```bash
git push origin --delete <branch-name>
```

## 📞 Контакты

Если возникли вопросы - создайте Issue в репозитории.

---

**Миграция завершена ✅**
